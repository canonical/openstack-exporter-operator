series: jammy

applications:
  openstack-exporter:
    charm: openstack-expoter # this will be overridden by overlay
    num_units: 1
  keystone:
    charm: keystone
    num_units: 1
  grafana-agent:
    charm: grafana-agent

relations:
- - openstack-exporter:cos-agent
  - grafana-agent:cos-agent
- - openstack-exporter:credentials
  - keystone:identity-admin
